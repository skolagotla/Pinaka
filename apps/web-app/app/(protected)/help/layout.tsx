/**
 * Help Center Layout
 * No additional layout needed - uses parent ProtectedLayoutWrapper
 */

export default function HelpLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

