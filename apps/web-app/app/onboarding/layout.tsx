/**
 * Onboarding Layout - Standalone layout for onboarding flow
 * No sidebar or navigation - clean, focused experience
 */
"use client";

import { ReactNode } from 'react';

export default function OnboardingLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

